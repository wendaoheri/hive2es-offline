elasticsearch:
  clusterNodes: localhost:9300
  clusterName: elasticsearch

curator:
  retryCount: 5
  elapsedTimeMs: 5000
  connectString: localhost
  sessionTimeoutMs: 5000
  connectionTimeoutMs: 5000
  rootPath: /es_offline
server:
  port: 0
workDir:
  /data/data01/es_offline,/data/data02/es_offline,/data/data03/es_offline
threadpools:
  process:
    corePoolSize: 4
    maxPoolSize: 4
  download:
    corePoolSize: 8
    maxPoolSize: 8
